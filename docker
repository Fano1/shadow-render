# Use a lightweight Alpine Linux base
FROM alpine:3.18

# Install Shadowsocks-libev
RUN apk add --no-cache shadowsocks-libev

# Expose Shadowsocks default port
EXPOSE 8388

# Start Shadowsocks server
CMD ss-server -s 0.0.0.0 -p 8388 -k ChangeThisPassword -m aes-256-gcm
